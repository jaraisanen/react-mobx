const size = {
  viewport4: "480px",
  viewport7: "768px",
  viewport9: "992px",
  viewport12: "1200px"
}

export const device = {
  viewport4: `(min-width: ${size.viewport4})`,
  viewport7: `(min-width: ${size.viewport7})`,
  viewport9: `(min-width: ${size.viewport9})`,
  viewport12: `(min-width: ${size.viewport12})`
}
